---
// @ts-ignore: Declared but never read.
import { getAbsoluteLocaleUrl } from 'astro:i18n'

// @ts-ignore: Declared but never read.
import { defaultLocale } from '~/i18n'

return Astro.redirect(getAbsoluteLocaleUrl(defaultLocale, '/'))
---
